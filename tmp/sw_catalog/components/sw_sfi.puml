@startuml

!global $swSfiActorList = ""

!procedure $addSwSfiActor( $type, $name, $desc )
    ' Declare the participant
    !if $type == "actor"
        actor $name
    !elseif $type == "database"
        database $name
    !else
        participant $name [
        =$name
        ----
        ""$desc""
        ]

    !endif
    
    ' Add its name to a global list string
    !global $swSfiListActor = $swSfiActorList + "# " + "**" + $name + "** - " + $desc +"\n"
!endprocedure

$addSwSfiActor( "participant", "IVI_SFI", "Safety Island ASIL-B" )

!if ( $HELP == true )
  note across : **Declared SW SFI Actors**:\n$swSfiActorList
!endif

@enduml